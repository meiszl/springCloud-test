server:
  port: 8000
spring:
  application:
    name: app-meiszl-member
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka/
    register-with-eureka: true
    fetch-registry: true